{
  "$schema": "https://json.schemastore.org/package.json",
  "name": "@opencode-ai/clangd-mcp",
  "version": "0.1.0",
  "description": "Standalone MCP server bridging clangd LSP to OpenCode agents",
  "type": "module",
  "license": "MIT",
  "private": true,
  "bin": {
    "clangd-mcp": "./dist/index.js"
  },
  "scripts": {
    "build": "bun build ./src/index.ts --outdir ./dist --target node --format esm && bun build ./src/bridge.ts --outdir ./dist --target node --format esm",
    "dev": "bun run ./src/index.ts",
    "test": "node test-clangd-mcp.mjs",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "1.25.2",
    "vscode-jsonrpc": "8.2.1",
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0"
  }
}
