{
  "$schema": "https://raw.githubusercontent.com/opencode-ai/intelgraph/main/schema.json",

  "enabled": true,
  "language": "typescript",
  "intelligence": "auto",

  "_comment_language": "c | cpp | typescript | javascript | rust — determines the intelligence backend",
  "_comment_intelligence": "auto (detect from language) | lsp (clangd) | graph (SQLite, run npm run extract first)",

  "server": "clangd",
  "args": [
    "--background-index",
    "--enable-config",
    "--log=error"
  ],

  "compileCommandsCleaning": {
    "preflightPolicy": "fix"
  }
}
